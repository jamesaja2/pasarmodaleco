# Database connection string (PostgreSQL)
# Example: postgresql://user:password@localhost:5432/pasar_modal_db
DATABASE_URL="postgresql://neondb_owner:npg_Zc3Q8WsIVBNp@ep-withered-bar-a1ggov7o-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# JSON Web Token secret (use a long random string)
JWT_SECRET="replace-with-secure-random-string"

# Admin bootstrap credentials
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="Admin123!"
ADMIN_TEAM_NAME="Enterprise Admin"
ADMIN_SCHOOL="Enterprise HQ"

# Default simulation configuration
SIMULATION_TOTAL_DAYS="15"
SIMULATION_STARTING_BALANCE="10000000"

# Optional redis cache (leave empty to use in-memory cache)
REDIS_URL=""

# Safe Exam Browser user agent fragment that must be present
DEFAULT_SEB_USER_AGENT="SafeExamBrowser"

# Comma separated list of allowed IP addresses (supports CIDR)
DEFAULT_ALLOWED_IPS="127.0.0.1/32"

# Websocket server port (defaults to 3001 if unset)
WEBSOCKET_PORT="3001"
